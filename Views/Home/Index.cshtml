@model IEnumerable<Financeiro>

    <div class="row mt-5">

        <div class="text-center">
            <a class="btn-success">Adicionar - Transação</a>
            <a class="btn-warning">Adicionar - Categorias</a>
            <a class="btn-info">Análise - Valores</a>
        </div>

        <div class="col-md-2">
            <div class="mb-3">
                <label class="form-label">Categoria</label>
                <select nome="filtro" class="form-select" asp-items="@(new SelectList(ViewBag.Catregorias,"CategoriaId","Nome",ViewBag.Filtros.CategoriaId))">
                    <option value="todos">Todos</option>
                </select>

            </div>
        </div>

        <div class="col-md-10">


        </div>

    </div>